# HackTheBox Writeup: Certified

**Difficoltà:** Easy
**OS:** Windows
**Date:** 07/04/2026

## 1. Enumeration
IP target: 10.129.24.119

### Port Scanning
We begin by mapping the open ports and services on the target using Nmap:

```
sudo nmap -sC -sV 10.129.231.186 -oA nmap/certified
```

We see SMB (on port 445 ), LDAP (on port 389 ), and Kerberos (on port 88 ) are running. Hence, we can identify this as a Domain Controller. From the Nmap scan results, we see that the domain
name is certified.htb , and the Domain Controller's DNS name is DC01.certified.htb . So, we add the domain name and DNS name to our /etc/hosts file.

Using the provided credentials judith.mader:judith09 , we enumerate the Domain Controller
using bloodhound:

